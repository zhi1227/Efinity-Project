# 团队仓库发布说明（2026-09-25）

本目录由用户复制到团队仓库。本次发布没有修改 RTL、重编译或烧录，核对原构建清单 505 项全部匹配。

保留：工程、引脚/时序约束、src/ip/tb/tools、说明、空样本模板，以及 contest_20260925/final 构建结果、final_sim 十二项日志、波形复核摘要/SVG和 board_run_20260925_144706 成功 JTAG 记录。

仅本地保留、不上传：编译/仿真缓存、历史 reports、contest/before 快照、Flash 原启动区备份、VCD/WDB、赛题/原理图渲染图片。没有删除这些本地文件。历史说明中的这些路径在干净克隆中可能不存在；可使用仓库历史版本或重新运行仿真，不把缺少旧备份误判为活动 RTL 缺失。

原 manifest 保留原始 SHA256，不重新伪造构建结果。最新硬件状态看 board_run，而不是下载前的 final/verification.json。Flash 未改，实物效果待确认。

发布清单例外：原 505 项包含 `ip/DdrCtrl/Ti180M484_devkit/work_pt/peri_res.json`，这是其他器件示例的接口生成缓存，不参与 Ti60 主工程。本次有意不上传这一项；其余 504 项全部进入 Git。原清单不篡改，干净克隆核对时只排除这个明确列出的缓存文件。

本目录使用 -text 保留源文件原始换行和构建哈希；不要对整个项目自动转换行尾。工具路径及日志中的 Windows 绝对路径属于原构建环境，新机器应自行配置 Efinity/XSim。

第三方和厂商 IP 保留原文件通知，不为它们附加新的开源许可。外部本机文档和其他尚未整理工程不属于本次发布。
